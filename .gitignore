node_modules
dist
*.local
*.tsbuildinfo
.DS_Store
.vite
# SQLite DB for the View-mode quality grid (created at runtime)
data/
*.db
*.db-journal
*.db-wal
*.db-shm
# Secrets — never commit. Examples (.env.example) ARE committed.
.env
.env.*
!.env.example
deploy/.env
deploy/.env.*
!deploy/.env.example
# Exported LLM API settings — contain provider keys, must never be committed
*-api-settings-*.json
# Local tooling / crash dumps
.antigravitycli/
bash.exe.stackdump
# Local STT verification server (tools/local-stt) — venv + model weights are machine-local
tools/local-stt/.venv/
tools/local-stt/models/
*.stackdump
# Per-team edit codes for the Config-mode Team editor — secret, ops-managed.
# The committed sample (deploy/team-codes.sample.json) shows the shape.
# Default dev location is <root>/data/team-codes.json (already covered by data/).
team-codes.json
